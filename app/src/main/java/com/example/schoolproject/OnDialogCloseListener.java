package com.example.schoolproject;

import android.content.DialogInterface;

public interface OnDialogCloseListener {
    //Triggered when a dialog fragment is dismissed
    void onDialogClose(DialogInterface dialog);
}
